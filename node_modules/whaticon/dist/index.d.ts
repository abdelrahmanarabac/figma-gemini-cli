export interface IconMatch {
    name: string;
    similarity: number;
}
export interface MatchOptions {
    /** Image size for comparison (default: 32) */
    size?: number;
    /** Maximum results to return (default: 10) */
    limit?: number;
    /** Minimum similarity threshold 0-1 (default: 0.8) */
    threshold?: number;
    /** Icon sets to search (default: all indexed) */
    prefixes?: string[];
    /** Preferred icon sets, sorted first at equal similarity */
    prefer?: string[];
}
export declare function svgToPixels(svg: string | Buffer, size?: number): Promise<Buffer>;
/**
 * Calculate similarity between two pixel buffers (0-1, higher = more similar)
 */
export declare function pixelSimilarity(px1: Buffer, px2: Buffer): number;
/**
 * Compute dHash (difference hash) for an image
 * Returns a Buffer of 128 bytes (1024 bits)
 */
export declare function computeDHash(svg: string | Buffer, size?: number): Promise<Buffer>;
/**
 * Calculate Hamming distance between two hash buffers using 32-bit popcount
 */
export declare function hammingDistance(h1: Buffer, h2: Buffer): number;
/**
 * Convert Hamming distance to similarity score (0-1)
 */
export declare function hashSimilarity(h1: Buffer, h2: Buffer): number;
/**
 * Fetch icon SVG from Iconify API
 */
export declare function fetchIconSvg(name: string): Promise<string>;
/**
 * Icon index with precomputed hashes (binary format)
 */
export interface IconIndex {
    names: string[];
    hashes: Buffer;
}
/**
 * Load index from binary files (gzipped)
 */
export declare function loadIndex(namesGz: Buffer, hashesGz: Buffer): IconIndex;
/**
 * Find matching icons from Iconify by visual similarity
 */
export declare function findMatches(svg: string | Buffer, index: IconIndex, options?: MatchOptions): Promise<IconMatch[]>;
interface IconInput {
    name: string;
    svg: string;
}
/**
 * Build index for a set of icons using sprite sheet batching (fast)
 * Returns buffers ready to be gzipped and saved
 */
export declare function buildIndex(icons: IconInput[], size?: number, onProgress?: (current: number, total: number) => void): Promise<{
    names: string;
    hashes: Buffer;
}>;
export {};
