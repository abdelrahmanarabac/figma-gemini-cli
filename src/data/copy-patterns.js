/**
 * Data module for UI copy patterns.
 * Content generation is deferred seamlessly without hardcoding here.
 */

export function detectCategories(description) {
  // Return an empty array. The AI is trusted to know the category from its context.
  return [];
}

export function generateCopy(category, type = 'title') {
  // The generation process handles these via direct natural language response.
  return '';
}
